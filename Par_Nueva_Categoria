const text = $input.first().json.text;
const match = text.match(/(?:nueva|agregar|crear)\s+categor[íi]a[:\s]+([\w\s]+)/i);
var nuevaCategoria;
if (match && match[1]) {
  nuevaCategoria = match[1].trim();
} else {
  nuevaCategoria = text.replace(/nueva|categoria|categoría|agregar|crear/gi, '').replace(/:/g, '').trim();
}
const inp = $input.first().json;
return [{ json: { text: inp.text, chatId: inp.chatId, username: inp.username, nuevaCategoria: nuevaCategoria } }];
